export interface User {
    uid: string;
}

export interface Identity {
    id: string;
    fullName: string;
    gender: string;
    age: string;
    connection: string;
    address: string;
    socialLinks: string;
    occupation: string;
    phone: string;
}
